{
  "dependencies": {
    "@hotwired/turbo-rails": "^8.0.0-beta.2",
    "@spree/dashboard": "^0.1.0",
    "esbuild": "^0.19.11"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --public-path=/assets"
  }
}
